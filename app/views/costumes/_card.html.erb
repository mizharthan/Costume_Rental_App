<div class="col-12 col-sm-6 col-md-4 col-lg-3 mb-4">
  <div class="card h-100 shadow-sm" style="min-height: 100%;">

    <% if costume.photo.attached? %>
      <%= cl_image_tag costume.photo.key,
            height: 200,
            width: 300,
            crop: :fill,
            class: "card-img-top img-fluid",
            alt: costume.name %>
    <% else %>
      <%= image_tag "https://via.placeholder.com/300x200?text=No+Image",
            class: "card-img-top",
            alt: "No image available" %>
    <% end %>

    <div class="card-body d-flex flex-column text-center">
      <h5 class="card-title mb-2"><%= costume.name %></h5>
      <p class="card-text small text-muted mb-2"><%= truncate(costume.description, length: 60) %></p>
      <p class="card-text fw-bold mb-3">€<%= '%.2f' % costume.price_per_day %>/day</p>
      <%= link_to "View details", costume_path(costume), class: "btn btn-outline-primary mt-auto" %>
    </div>
  </div>
</div>
